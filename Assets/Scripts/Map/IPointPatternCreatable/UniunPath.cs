namespace Assets.Scripts.Map
{
    public class UniunPath // IPointPatternCreatable
    {
        //public List<PointOfInterestGenerator.InteresPointEntity> Create(
        //    ref List<PointOfInterestGenerator.InteresPointEntity> lastLevelPoint,
        //    List<InterestingPointConfig> pointsSet)
        //{
        //    if (lastLevelPoint.Count != 2)
        //        throw new Exception("Union must have 2 point of interest!");

        //    var index = UnityEngine.Random.Range(0, pointsSet.Count);
        //    var newPoint = new PointOfInterestGenerator.InteresPointEntity()
        //    {
        //       // InteractivePoint = pointsSet[index].InteractivePoint,
        //    };

        //    lastLevelPoint.ForEach(point =>
        //    {
        //        point.ConnectPoints.Add(newPoint.InteractivePoint);
        //    });


        //    return new List<PointOfInterestGenerator.InteresPointEntity>() { newPoint };
        //}
    }
}